defaults:
  - _self_
  - launcher/local

hydra:
  output_subdir: null
  mode: MULTIRUN
  run:
    dir: .

env_name: "swm/MountainCarContinuousControl-v0"
cache_dir: null

world:
  num_envs: 16
  max_episode_steps: 1000
  image_shape: [224, 224]

seed: 3072
num_traj: 10000

options: null

device: cuda
render_mode: rgb_array
ckpt_path: /home/mamad/PhD/stable-worldmodel/scripts/expert/train_mountain_car/best_model/best_model