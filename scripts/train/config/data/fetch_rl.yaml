dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}  # 4 with default wm.*
  frameskip: 5
  name: fetch_reach_expert_rl_agent.lance
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state