dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}  # 4 with default wm.*
  frameskip: 5
  name: fetch_reach_expert.lance
  keys_to_load:
    - action
    - state
  keys_to_cache:
    - action
    - state